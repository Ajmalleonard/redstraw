# @RedStraw/example

> Framework Agnostic example of [RedStraw](https://github.com/zce/RedStraw) with most features.

## Usage

```shell
$ npm install # install dependencies
$ npm run dev # run build with watch mode
$ npm run build # build content by RedStraw
```
